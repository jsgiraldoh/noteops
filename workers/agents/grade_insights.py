"""
grade_insights.py — Agente de análisis de notas (placeholder).
Futuro: análisis con LLM de patrones en notas estudiantiles.
"""
import os, time, logging
logging.basicConfig(level=logging.INFO)
log = logging.getLogger(__name__)

def main():
    log.info("Workers IA iniciados — modo placeholder")
    while True:
        log.info("Analizando notas... (implementación pendiente)")
        time.sleep(60)

if __name__ == "__main__":
    main()
